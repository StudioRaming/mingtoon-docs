---
id: v0.1.3
title: 0.1.3
sidebar_position: -103
slug: /changelog/0.1.3
---

<!--
  Authoring rules live in the comment at the top of docs/changelog/_template.md.
  Always fill all three locales (ko / en / ja). Leaving one empty makes that
  language fall back to Korean, which looks translated but is not.
  Link anchors stay in the original Korean across all three languages.
-->

*Closed beta*

## Check before you upgrade

:::danger[Do not delete MingToon components by hand]
VRChat **strips them on upload** by itself. Deleting them manually also drops the cached renderer list, so the **upload optimisation loses its scope** and unoptimised materials ship as-is. → [VRChat](/platforms/vrchat)
:::

:::caution[Auto face detection no longer guesses from names]
`Auto` now reads **only the face flag the source material carries**. Converting from Standard or URP Lit leaves **every slot as `Regular`**, so you have to mark the face slots as `Face` yourself. → [MingToon Manager](/workflow/character-manager#얼굴--피부-지정--가장-중요한-단계)
:::

:::caution[The manager's step numbers changed]
The order is now `1 · Convert` → `2 · Setup` → `3 · Advanced`. Older notes will not match.
:::

## New

- **Depth Translucency** — thin parts such as hair tips and hems now look like they are holding light. It measures how far light travelled through the character by reading depth twice along the light direction, so it works on its own regardless of `Master Width`. → [Docs](/guides/depth-effects#깊이-투과광)
- **Upload readiness check** — walks the hierarchy and the manifest and reports anything blocking an upload as a diagnostic code. → [Docs](/workflow/character-manager#업로드-준비-점검)
- **Status panel** — material composition, shading state, face-slot sync and optimisation scope are always visible at the top of the manager.
- **VRChat and Quest pre-checks** — counts missing standard properties and disallowed components, and reports what a Quest variant would lose.
- **`Skin` role** — a slot role that receives the bare-skin look values. `Assign Skin Renderer Directly` sets it in one go.
- **Tab presets** — save and apply presets per section, with an option to include textures.
- **WARUDO Runtime Root** — a self-contained runtime component that supplies camera depth and head direction to the shader in a Warudo mod. → [Warudo](/platforms/warudo#내보내기-전-필수--warudo-runtime-root)

## Changed

- **Conversion is no longer lilToon-only** — Standard, URP Lit and others convert too. In that case base colour, textures, normal, emission, occlusion and render state carry over, and everything else takes MingToon defaults.
- **Excluded material classes** — particle/VFX, refraction/gem, fur/shell, audio-reactive, flipbook/scroll, and outline-only or auxiliary passes cannot be carried over, so their originals are left untouched. → [Docs](/workflow/character-manager#변환되지-않는-슬롯)
- **Hull normal source now defaults to `UV8TS`** — baking UV8 is the whole job; there is nothing to switch on the material afterwards. → [Docs](/guides/mesh-bakes#아웃라인-스무스-노멀-uv8)
- **`Character Root` is now called `MingToon Manager`.**
- **`lilToon Quick Convert` is now called `Shader Conversion`.**
- **What a preset carries** — values and colours only. Textures, vectors, render queue, instancing and the RenderType tag are left alone.
- **The Face SDF Baker ships as a separate editor package.** Without it installed, that button stays closed.

## Improved

- **UV channel occupancy** — shows whether `UV4 · UV7 · UV8` are free or in use, and warns that UV7 is shared between the face normal bake and the face SDF projection, so only one can own it.
- **Conversion pair audit** — verifies converted materials against their sources. It is slow, so it runs only when you press the button.
- **Face module sync** — warns when a face slot's keyword disagrees with its material value and fixes them in one action. Baking in that state drops face shading.
- **Optimisation scope warning** — tells you how many renderers would upload unoptimised when the cache is stale.
- **Editor runtime preview** — preview look profiles and runtime face direction in the editor. None of it ships to a VRChat upload.

## Docs

- The documentation site is now available in **한국어 · English · 日本語**. Switch at the top right.
- New **Internals** section — shader pass structure, the performance cost formula, what the bake removes, how to read a conversion report, and the VRChat compatibility rules. → [Docs](/internals/shader-structure)
- The control reference is generated from the MingToon source, so it always matches what the inspector displays.
- Added full-text search.
