---
id: vTEMPLATE
title: TEMPLATE
sidebar_position: 0
---

<!--
===============================================================================
  패치노트 작성 규칙  —  이 파일은 템플릿입니다. 직접 편집해서 배포하지 마세요.
===============================================================================

파일 이름이 `_` 로 시작하므로 사이트에 나오지 않습니다.

■ 새 버전을 추가하는 방법

    node scripts/new-patch-note.mjs 0.1.4

  이 명령이 한국어 · 영어 · 일본어 세 파일을 이 템플릿에서 만들어 줍니다.
  frontmatter(id · title · sidebar_position)는 스크립트가 계산해 채웁니다.
  손으로 만들지 마세요. sidebar_position 을 틀리면 버전 순서가 뒤집힙니다.

■ 반드시 지킬 것

  1. 세 언어를 항상 함께 채웁니다.
     한국어만 쓰고 두면 영어·일본어 페이지가 한국어로 폴백되어, 번역이 된
     것처럼 보이면서 실제로는 안 된 상태가 됩니다.

  2. 섹션은 아래 순서를 지킵니다. 해당 없는 섹션은 통째로 지웁니다.
     빈 제목만 남겨 두지 마세요.

       ## 업그레이드 시 확인할 것   ← 있으면 반드시 맨 위
       ## 새 기능
       ## 변경
       ## 개선
       ## 수정
       ## 문서

  3. `업그레이드 시 확인할 것` 은 **기존 작업물이 깨지거나 동작이 달라지는
     것만** 적습니다. 신규 기능은 여기 넣지 않습니다.
     읽는 사람이 "내 아바타에 무슨 일이 생기나"만 알면 되게 씁니다.

  4. 각 항목은 한 줄로 끝냅니다. 배경 설명이 필요하면 문서 페이지로 링크하고
     여기서는 결론만 씁니다. 패치노트는 읽는 곳이지 설명하는 곳이 아닙니다.

       좋음:  **깊이 투과광** 추가 — 얇은 부위가 빛을 머금습니다. → [문서](/guides/depth-effects#깊이-투과광)
       나쁨:  깊이 투과광이라는 기능을 추가했는데 이것은 광원 방향으로 깊이를
              두 번 읽어서 ... (설명이 길면 문서로 보내세요)

  5. UI 라벨은 인스펙터에 실제로 표시되는 문구를 백틱으로 감쌉니다.
     한국어판에는 한국어 라벨, 영어판에는 영어 라벨을 씁니다.
     라벨 대조표: MingToon 소스의 Editor/InspectorUx/MingInspectorText.cs

  6. 링크 앵커는 **한국어 원본 그대로** 씁니다. 영어·일본어 페이지에서도
     같은 앵커를 그대로 복사하세요. 앵커는 언어와 무관하게 고정되어 있습니다.

       한국어:  → [문서](/guides/depth-effects#깊이-투과광)
       영어:    → [Docs](/guides/depth-effects#깊이-투과광)   ← 앵커 동일

  7. 존재하지 않는 앵커를 쓰면 빌드가 실패합니다(onBrokenAnchors: throw).
     쓰기 전에 대상 문서에 그 제목이 있는지 확인하세요.

  8. Admonition 은 v3 문법만 씁니다.  :::caution[제목]  … 대괄호 필수.
     `:::caution 제목` 처럼 쓰면 화면에 `:::caution` 이 그대로 찍힙니다.

■ 다 쓴 뒤

    npm run build            # 세 로케일 + 링크·앵커 검사
    node scripts/check-translations.mjs en
    node scripts/check-translations.mjs ja

  세 명령이 모두 통과해야 배포합니다.

■ 날짜

  제목 아래 첫 줄의 날짜는 실제 배포일로 바꾸세요. 스크립트가 채우지 않습니다.
===============================================================================
-->

*릴리스: YYYY-MM-DD*

## 업그레이드 시 확인할 것

<!-- 기존 작업물이 깨지거나 동작이 달라지는 것만. 없으면 이 섹션을 지우세요. -->

:::caution[제목]
설명 한두 줄.
:::

## 새 기능

- **기능 이름** — 한 줄 설명. → [문서](/경로)

## 변경

- 무엇이 어떻게 바뀌었는지 한 줄.

## 개선

- 한 줄.

## 수정

- 한 줄.

## 문서

- 한 줄.
