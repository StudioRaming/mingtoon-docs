---
id: vHOTFIX-TEMPLATE
title: HOTFIX TEMPLATE
sidebar_position: 0
description: <한 줄 요약과 같은 문장>
---

<!--
===============================================================================
  핫픽스 노트 템플릿 — 직접 편집해서 배포하지 마세요.
  파일 이름이 `_`로 시작하므로 사이트에 나오지 않습니다.
===============================================================================

■ 핫픽스란

  버전 번호를 올리지 않고 같은 버전의 패키지를 다시 올린 수정입니다.
  정식 릴리스 노트와 섞지 않고 버전당 핫픽스 페이지 하나에 차수별로 쌓습니다.
  파일 이름은 `<버전>-hotfix.md`, 세 언어 모두 같은 이름입니다.
  새 차수는 항상 이 페이지 맨 위에 붙입니다.

■ frontmatter

  id:               v<버전>-hotfix
  title:            <버전> 핫픽스   (영어: <버전> Hotfix / 일본어: <버전> ホットフィックス)
  sidebar_position: 릴리스 노트의 값에서 0.5를 뺀 값
                    (0.1.7이 -107이면 핫픽스는 -107.5)
  slug:             /changelog/<버전>-hotfix
  description:      한 줄 요약과 같은 문장

■ 반드시 지킬 것

  1. 세 언어를 항상 함께 채웁니다. 한국어만 쓰면 나머지가 한국어로 폴백됩니다.
  2. 릴리스 노트에서 핫픽스 항목을 빼고 이 페이지로 옮깁니다.
     릴리스 노트 끝에는 핫픽스 페이지 링크 한 줄만 남깁니다.
  3. `업데이트 전 확인`에는 재업로드·재변환·직접 되돌릴 값만 적습니다.
     한 목록 안에서 "재업로드가 필요하다"와 "필요 없다"를 함께 쓰지 마세요.
     대상 재질을 명시해 하나로 정리합니다.
  4. 각 항목은 증상 문장으로 씁니다. 원인 설명은 한 줄까지만 붙입니다.
  5. 설치 경로처럼 이후 버전에서 달라진 안내는 `:::note[지금은]` 한 블록으로 알립니다.
  6. UI 라벨은 인스펙터 문구 그대로, 앵커는 한국어 원본 그대로.
     한 문장 60자 이내, 한 페이지 80줄 이내, 이모지 금지.
  7. 아직 배포하지 않은 차수는 커밋만 하고 푸시하지 않습니다.
     사이트는 푸시 즉시 배포됩니다.

■ 다 쓴 뒤

    npm run build
    node scripts/check-translations.mjs en
    node scripts/check-translations.mjs ja
===============================================================================
-->

*X.Y.Z 핫픽스 N · YYYY년 M월 D일 · 패키지 버전은 X.Y.Z 그대로*

## 한 줄 요약

<이번 핫픽스로 사용자가 얻는 것 한 문장. 40자 이내>

:::warning[업데이트 전 확인]
- 받는 법: 같은 X.Y.Z 패키지를 다시 받아 덮어쓰면 됩니다.
- 재업로드: <필요한 재질을 명시. 없으면 "필요 없습니다.">
- 재변환: <필요한 재질을 명시. 없으면 "필요 없습니다.">
:::

:::info[핫픽스 읽는 법]
버전 번호를 올리지 않고 같은 패키지를 다시 올린 수정입니다.
몇 차까지 들어 있는지는 패키지 안 `Docs/PATCH_NOTES_X.Y.Z.md`로 확인합니다.
:::

## 수정된 문제

- <증상 문장>이 고쳐졌습니다.
- <원인이나 조치 한 줄. 필요할 때만.>

## 알려진 제한

- <아직 안 되는 것>. <대신 할 수 있는 것>.

[설치·업데이트 안내](/getting-started/installation) · [X.Y.Z 패치노트](/changelog/X.Y.Z)
