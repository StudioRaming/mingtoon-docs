---
id: vHOTFIX-TEMPLATE
title: HOTFIX TEMPLATE
sidebar_position: 0
---

<!--
===============================================================================
  핫픽스 노트 작성 규칙  —  이 파일은 템플릿입니다. 직접 편집해서 배포하지 마세요.
===============================================================================

파일 이름이 `_` 로 시작하므로 사이트에 나오지 않습니다.

■ 핫픽스란

  버전 번호를 올리지 않고 같은 버전의 패키지를 다시 올린 수정입니다.
  정식 릴리스 노트(예: 0.1.7.md)와 섞지 않고 버전당 핫픽스 페이지 하나에
  차수별로 쌓습니다. 파일 이름은 `<버전>-hotfix.md`, 세 언어 모두 같은 이름.

■ frontmatter

  id:               v<버전>-hotfix
  title:            <버전> 핫픽스        (영어: <버전> Hotfix / 일본어: <버전> ホットフィックス)
  sidebar_position: 릴리스 노트의 sidebar_position 에서 0.5 를 뺀 값
                    (0.1.7 이 -107 이면 핫픽스는 -107.5 → 빌드 결과 목록에서
                    0.1.7 바로 다음, 0.1.6 앞에 붙는 것을 확인함)
  slug:             /changelog/<버전>-hotfix

■ 본문 구조 (새 차수는 항상 맨 위에)

  *<버전> 핫픽스 · 패키지 버전은 <버전> 그대로*

  :::info[핫픽스 읽는 법]
  같은 버전을 다시 받아 덮어쓰면 됩니다. 몇 차인지는 패키지 안 Docs/PATCH_NOTES 로 확인.
  :::

  ## 핫픽스 N — YYYY-MM-DD (N차)

  **업데이트 후 해야 할 것**        ← 없으면 "없음. 덮어쓰기만 하면 됩니다." 한 줄
  - 재업로드가 필요한지, 재변환이 필요한지, 손으로 되돌릴 값이 있는지만.

  ### 항목 제목 (증상을 그대로 제목으로)
  - 증상 — 사용자가 본 것 한 줄.
  - 조치 — 무엇을 어떻게 고쳤는지 한 줄. 배경은 문서 링크로.

■ 반드시 지킬 것

  1. 세 언어를 항상 함께 채웁니다. 한국어만 쓰면 영어·일본어가 한국어로 폴백됩니다.
  2. 릴리스 노트(0.1.7.md)에서 핫픽스 항목을 지우고 이 페이지로 옮깁니다.
     릴리스 노트 끝에는 핫픽스 페이지 링크 한 줄만 남깁니다.
  3. 아직 배포되지 않은 차수는 커밋만 하고 푸시하지 않습니다. 사이트는 푸시 즉시 배포됩니다.
  4. UI 라벨은 인스펙터 문구를 백틱으로, 앵커는 한국어 원본 그대로 — 릴리스 노트 규칙과 같습니다.

■ 다 쓴 뒤

    npm run build
    node scripts/check-translations.mjs en
    node scripts/check-translations.mjs ja
===============================================================================
-->

*X.Y.Z 핫픽스 · 패키지 버전은 X.Y.Z 그대로*

:::info[핫픽스 읽는 법]
버전 번호를 올리지 않고 같은 X.Y.Z 패키지를 다시 올린 수정입니다. BOOTH에서 다시 받아 `Assets/StudioRaming/MingToon`에 덮어쓰면 됩니다.
:::

## 핫픽스 1 — YYYY-MM-DD (1차)

**업데이트 후 해야 할 것**

- 없음. 덮어쓰기만 하면 됩니다.

### 항목 제목

- 증상 — 한 줄.
- 조치 — 한 줄.
